<% provide(:title, '会員登録') %>
<h1>Youtuber専用</h1>
<p>会員登録すると自分のyoutube動画を投稿できます。</p>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation, "再入力" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "アカウントを作成", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>