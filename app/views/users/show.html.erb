<% provide(:title, "#{@user.name}のプロフィール") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <section>
    <p>
      <span><%= @user.name %></span>
      <% if (logged_in? && @user == current_user) || (logged_in? && admin_user?) %>
      <%= link_to "編集する",edit_user_path(@user) %>
      <%end%>
    </p>
    </section>
  </div>
</div>

<hr>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <section>
    <p><%= render "shared/youtube" %></p>
    </section>
  </div>
</div>